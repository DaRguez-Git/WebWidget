<?xml version="1.0" encoding="utf-8"?>
<!--
    RemoteViews layout for the home-screen widget.

    StackView is the RemoteViews-friendly equivalent of a swipeable
    pager: each child fills the StackView (full widget area) and the
    user swipes vertically/horizontally to bring the next card to the
    front. This is the same component the legacy "Bookmarks" widget
    used. The launcher handles gesture detection for us.

    The TextView is the empty view (setEmptyView). It is shown
    automatically whenever the StackView reports zero items — i.e.
    before the first snapshot lands, on a freshly placed widget.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background">

    <StackView
        android:id="@+id/widget_stack"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:loopViews="true" />

    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:padding="12dp"
        android:textColor="@color/widget_error_text"
        android:textSize="14sp"
        android:text="@string/widget_loading" />

</FrameLayout>
