# Default Android proguard rules. WorkManager and Compose handle their own
# keep-rules through consumer-rules in their AARs, so nothing extra is needed
# while minification is disabled. Add app-specific rules here if R8 is enabled.
